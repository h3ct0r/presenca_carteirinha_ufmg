# Custom 16MB layout: huge app + dedicated partition for esp-dl .espdl models.
# The human_face_det label is hardcoded in the human_face_detect component
# (model location = flash partition); tools/build/pack_models.py builds its image.
#
# 256 KB for a ~187 KB payload, not the 1 MB first reserved here: FbsLoader mmaps
# the WHOLE partition, and MSR and MNP each construct their own loader, so the
# partition is mapped twice. The gap up to spiffs is deliberate — shrinking in
# place keeps every other offset where it already is on flashed devices.
# Name,          Type, SubType,  Offset,   Size,     Flags
nvs,             data, nvs,      0x9000,   0x5000,
otadata,         data, ota,      0xe000,   0x2000,
app0,            app,  ota_0,    0x10000,  0x600000,
human_face_det,  data, 0x82,     0x610000, 0x40000,
spiffs,          data, spiffs,   0x710000, 0x8E0000,
coredump,        data, coredump, 0xFF0000, 0x10000,
