#pragma once

#include "ui/screen.h"

// Staff: "new class" form (code, name, schedule). Demo only — nothing is
// persisted yet; the storage service will hook in via the event bus later.
extern const screen_t scr_class_form;
