#pragma once

#include "ui/screen.h"

// Staff: teacher registration / card re-binding form (dark hero layout from
// the legacy login flow). Reached from the account screen; in the final flow
// it will also open when an unknown card is presented at the idle screen.
extern const screen_t scr_teacher_reg;
