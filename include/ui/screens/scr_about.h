#pragma once

#include "ui/screen.h"

// About screen: what the project is, who made it, who supported it, which
// firmware build is running, and a scannable QR to the repository. Reachable
// from the Admin panel; the idle screen shows the version string only.
extern const screen_t scr_about;
